// Ambiente de desenvolvimento. O `ng serve` usa proxy.conf.json para
// encaminhar /api ao backend local (http://localhost:3000 por padrão).
export const environment = {
  production: false,
  apiUrl: "/api",
};
